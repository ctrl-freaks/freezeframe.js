{
  "name": "freezeframe-root",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "test": "lerna run test --stream",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream"
  },
  "devDependencies": {
    "eslint-plugin-import": "^2.15.0",
    "lerna": "^3.20.2"
  }
}
